from datetime import date
  
def calculate_age(dob):
    days_in_year = 365.2425    
    age = int((date.today() - dob).days / days_in_year)
    return age
          

print(calculate_age(date(2002, 10, 01)), " years old")
